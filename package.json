{
  "name": "soksak-plugin-vtuber",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "soksak 브이튜버 플러그인 — Live2D 아바타 컴패니언(표정·자막·음성 대화)",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "license-gate": "node scripts/license-gate.mjs"
  },
  "dependencies": {
    "pixi-live2d-display-lipsyncpatch": "0.5.0-ls-8",
    "pixi.js": "^7.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.24.2",
    "typescript": "~5.9.3",
    "vitest": "^2.1.8"
  }
}
